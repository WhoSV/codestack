// API ROOT
let API_ROOT = 'http://127.0.0.1:8000';

// AUTH
export const AUTH_URL = `${API_ROOT}/auth`;

// USERS
export const USERS_URL = `${API_ROOT}/people`;

// COURSES
export const COURSES_URL = `${API_ROOT}/courses`;

// FAVORITE
export const FAVORITE_URL = `${API_ROOT}/favorite`;
